# Front e-depot — variables de build (Vite, injectées à la compilation).
# En dev : laisser vide → le proxy Vite route /api vers http://localhost:3001.
# En prod (Netlify) : URL publique de l'API Render.
VITE_API_URL=""
